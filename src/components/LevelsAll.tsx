import React from 'react';
import LevelSelectorAll from '@/components/LevelSelectorAll';

const Levels = () => {
  return (
    <div className="bg-gray-50 min-h-screen">
      <LevelSelectorAll />
    </div>
  );
};

export default Levels;